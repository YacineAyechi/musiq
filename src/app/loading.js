import { OrbitProgress } from "react-loading-indicators";

const Loading = () => {
  return (
    <OrbitProgress
      variant="disc"
      color="#000000"
      size="medium"
      text=""
      textColor=""
    />
  );
};

export default Loading;
